﻿
@model ProductIndexVM

<h1>Product page</h1>
<br />

<a asp-action="Add" class="btn btn-success">Add</a>
<br />
<br />

<div class="row">
    @foreach (var product in Model.Products)
    {
        <div class="col-4">
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="~/img/featured/@product.ImageUrl" height="300">
                <div class="card-body">
                    <h5 class="card-title">@product.Name</h5>
                    <p class="card-text">Price: @product.Price.ToString(".##")</p>
                    <a asp-action ="Update" asp-route-id="@product.Id" class="btn btn-warning">
                        Update</a>
                    <a asp-action="Delete" asp-route-id="@product.Id" class="btn btn-danger">Delete</a>
                </div>
            </div>
        </div>
    }
</div>